let searchBtn = document.getElementById("search-btn");

let counteyInp = document.getElementById("country-inp");
searchBtn.addEventListener("click", () => {
  let countryName = counteyInp.Value;
  let finalURL = ``;
});
